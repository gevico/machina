[package]
name = "machina-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
machina-core = { path = "../core" }
machina-backend = { path = "../backend" }
machina-frontend = { path = "../frontend" }
machina-exec = { path = "../exec" }
machina-decode = { path = "../decode" }
libc = "0.2"
